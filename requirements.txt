# Web Scraping
requests==2.31.0
beautifulsoup4==4.12.2
lxml==5.3.0

# Backend (Flask)
Flask==3.0.0
Flask-Cors==4.0.0
Flask-SQLAlchemy==3.0.5
Flask-JWT-Extended==4.6.0

# Database (PostgreSQL or SQLite)
psycopg2-binary==2.9.9  # PostgreSQL (use this if using PostgreSQL)
# sqlite3  # For SQLite (built-in with Python)

# Machine Learning (for Reinforcement Learning)
numpy==1.26.3
scipy==1.11.4
pandas==2.1.4
torch==2.1.2  # If using PyTorch for RL
tensorflow==2.14.0  # If using TensorFlow instead of PyTorch
scikit-learn==1.3.2  # For data preprocessing & analytics

# API & Async Requests (if needed for further scraping)
aiohttp==3.9.1  # For async web scraping
selenium==4.16.0  # If dynamic content requires automation
webdriver-manager==4.0.1  # For managing browser drivers

# Testing & Debugging
pytest==7.4.3
pytest-flask==1.3.0

# Security & Authentication
bcrypt==4.1.2
python-dotenv==1.0.0  # To load environment variables (e.g., DB credentials)

# Data Visualization (Optional)
matplotlib==3.8.2
seaborn==0.13.0
